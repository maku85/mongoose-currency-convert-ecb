{
  "require": ["ts-node/register"],
  "extension": ["ts"],
  "spec": ["test/**/*.spec.ts"],
  "recursive": true,
  "timeout": 10000,
  "exit": true,
  "parallel": false,
  "check-leaks": true,
  "reporter": "spec",
  "watch-files": ["src/**/*.ts", "test/**/*.ts"],
  "watch-ignore": ["node_modules", "dist"]
}
